# Deprecated: production uses compose.production.yaml and one app process.
# Do not install or enable this unit. Splitting HTTP, WebSocket, and workers
# into separate Node processes breaks the shared in-memory room lifecycle.
