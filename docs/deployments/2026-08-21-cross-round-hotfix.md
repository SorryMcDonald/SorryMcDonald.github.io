# 炸金花跨局结算热修复与房间清退回执

## 热修复发布

- 发布时间：2026-08-21（Asia/Shanghai）
- 公网地址：`https://crazythursdayplay.bbroot.com/`
- 应用提交：`7207c25ffcdbd1b6b00e94f9823642766b5c2364`
- 发布标识：`20260821T094756Z-7207c25ffcdb`
- 发布归档 SHA-256：`8b035e64f6ef49d2a76f02292d3f563e5910c4ba8514ed1388709e1972effda9`
- 上一应用提交：`06e390a5ee4e2fba808d3bd543bd91d8ba6b5b2a`
- 数据库迁移：无，继续使用 schema level 10
- 最终 recovery manifest SHA-256：`40036e47e2aa6286b526bfd2648fa809ac1a606dff80d60eb4d0931b3a304893`

## 修复内容

- 新一局开始前清理所有保留玩家记录中的局内字段，包括已离开玩家的上一局贡献、牌面、动作序号和结算视图字段。
- 当前局离开的玩家仍保留本局合法贡献，由正常结算和边池逻辑处理。
- 新增跨两局回归：玩家离场、剩余玩家比牌、下一局看牌跟注、再次离场时必须正常结算且底池等于本局贡献。

## 自动化验证

- Vitest：`27` 个文件、`185` 项测试通过。
- Playwright：`8` 项测试通过。
- Node 语法检查：`37` 个文件通过。
- 托管工作流严格校验：通过。
- 候选基于当前生产镜像离线构建，并在隔离恢复的最新生产数据库上通过 `/healthz`。

## 生产清退

- 两个请求标识中，生产用户表唯一匹配 `1` 个，另一个精确邮箱和本地部分匹配均为 `0`。
- 匹配账号最初同时占有 `1` 个炸金花房间和 `1` 个斗地主房间。
- 使用生产服务层执行正式离房：炸金花按当前局事件保留合法贡献并完成结算，斗地主等待座位标记离开。
- 为防止客户端立即自动重入，撤销了该账号当时的 `3` 个活跃会话；未禁用账号、未修改密码。
- 最终数据库验证：两个请求标识的炸金花、德州和斗地主活跃占座合计为 `0`。
- 账号随后产生的 `1` 个新会话也已撤销；最终活跃会话为 `0`，活跃占座仍为 `0`。
- 清退前创建并校验 PostgreSQL 自定义格式备份；最终一次清退备份 SHA-256：`cc09731676735f8216bf365a118975e63dbc78b95739aa8d6af00900b65b18dd`。
- 最终清退结果收据 SHA-256：`a7608fab7c3db0d0620b826ebfc446e591b34ceebc6bd376ac20876ba06b5baf`。

## 运行验收

- 应用容器运行精确候选镜像，OCI revision 标签等于应用提交。
- 应用容器持续 `healthy`，重启计数为 `0`。
- 发布后完成两次间隔 30 秒的健康采样，镜像和重启计数未漂移。
- Edge 公网锦标赛页面刷新成功，特殊赛内容可见，控制台无应用错误。
- 服务器本机访问公网域名存在 hairpin 超时，Windows `curl` 仍存在 Schannel 限制；两者未用于外网可用性结论。

## 连接状态

- 云防火墙和主机 nftables 均允许所有 IPv4 来源访问 TCP/22。
- `maintain` 使用独立 ED25519 运维密钥并强制 key-only；root 登录策略未扩大。
- 客户端和服务端均为 30 秒 keepalive、12 次无响应容忍，并启用 TCP keepalive。
- key-only 登录、无密码最小 sudo 和 75 秒空闲长连接均验证通过。
