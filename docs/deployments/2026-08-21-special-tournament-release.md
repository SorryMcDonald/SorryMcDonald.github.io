# 炸金花与特殊赛生产发布回执

## 发布信息

- 发布时间：2026-08-21（Asia/Shanghai）
- 公网地址：`https://crazythursdayplay.bbroot.com/`
- 应用提交：`06e390a5ee4e2fba808d3bd543bd91d8ba6b5b2a`
- 发布标识：`20260821T082906Z-06e390a5ee4e`
- 发布归档 SHA-256：`fb25a82e954257ab512886c065c6a15a4b65d39465370222a3ec73fcc79db7d9`
- 上一应用版本：`20260820T153326Z-669183c7ef5d`
- 数据库迁移：level 9 -> level 10
- 迁移 010 SHA-256：`ae41a52f5785512d819e089c9a3e1cd0cb1cc52917bf5d33fdf80f263a30f1eb`

## 修复范围

- 癞子炸金花的直接比牌和边池结算统一使用最佳癞子牌型。
- 特殊德州报名后按后端 `gamePath` 进入正确牌桌。
- 虚拟筹码报名回滚不再产生无对应扣款的退款账本。
- 特殊德州换街和进程恢复后保持 1,000 最低加注。
- 常驻赛在开赛前拒绝手动开始，到达场次边界后由生命周期队列自动开始第一局。
- 缺失游戏房间只隔离当前赛事桌错误，不中断全局赛事调度器。

## 连接修复

- 云防火墙和主机防火墙均允许所有 IPv4 来源访问 TCP/22。
- `maintain` 使用独立 ED25519 运维密钥并禁用密码认证；root 登录策略未扩大。
- 客户端和服务端均配置 30 秒 keepalive、12 次无响应容忍和 TCP keepalive。
- key-only 别名登录通过；75 秒空闲 SSH 会话跨越两个探活周期后正常结束。

## 发布与回滚证据

- 最终 recovery manifest SHA-256：`8bb14af44168bfa1e8be3f7e345a9a7c50dbc689242204b639d9e0ce77859f26`
- 隔离候选 probe receipt SHA-256：`5e3ef370e6a1c3b004dbea55f7b12efbc9d079b4cd4f5c39721b49a5c372e6b9`
- 生产 schema 10 receipt SHA-256：`69f9faca70e5dac264ec1de19eaf1770a843d8528f1991a8d67ad1e4a90039bc`
- 规范化 schema 10 SHA-256：`27215944dd13d423174e337fcead29775a6dc6ba578f4497d20ba4e376442344`
- 发布前旧镜像、数据库自定义格式备份、旧 Compose、schema 9 receipt 和 6 个共享入口配置哈希均已验证。
- 候选先在隔离 PostgreSQL 恢复生产备份，执行迁移 010、schema verifier 和应用健康检查，再进入生产迁移。
- 应用通过原子 symlink 和 Compose `app` 单服务切换；数据库、Nginx 和 Cloudflared 未重建。
- 应用回滚恢复上一 symlink、上一镜像和哈希验证的旧 Compose；level 10 schema 保留。数据库 dump 仅用于另行批准的灾难恢复，避免丢失发布后写入。

## 验证结果

- Vitest：`27` 个文件、`183` 项测试通过。
- Playwright：`8` 项测试通过。
- Node 语法检查：`37` 个文件通过。
- 生产依赖审计：`0` 个漏洞。
- 托管工作流严格校验：通过。
- 四个关键容器均为 `running/healthy`，发布前后重启计数为 `0`。
- Nginx 配置检查和 Cloudflared 运行检查通过，6 个共享入口配置哈希未变化。
- 容器内 `/healthz` 返回 HTTP 200。
- 公网真实验收通过：2 个临时账号、双人房间、WSS `round_started`、排行榜、常驻特殊赛路径、最新版锦标赛脚本和退出后房间回收。
- 临时验收数据已精确清理：用户剩余 `0`，房间剩余 `0`。
- 发布后 60 秒内完成 3 次采样，应用持续健康、重启计数为 `0`、镜像未漂移。
- Edge DOM 和控制台检查确认牌局大厅与常驻特殊赛页面正常、无应用错误；截图接口两次超时，未作为可用性结论依据。

## 残余边界

- “任意网络环境”由云/主机 `0.0.0.0/0` 规则、无来源限制公钥和 keepalive 配置证明；实际连接 canary 来自当前外部网络，未逐一覆盖所有运营商/NAT 类型。
- 主机提示存在待安装系统更新并需要重启，本次应用发布未扩大为 OS 升级或主机重启。
