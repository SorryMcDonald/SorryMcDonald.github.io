# 炸金花联机版修改说明

## 已修复 Bug

- 移除了 10 秒操作超时和超时自动弃牌；当前没有任何会因等待时间改变牌局状态的计时器。
- 移除了前端操作倒计时，避免界面仍提示不存在的超时规则。
- 修复“开始下一局”后旧胜者结算弹窗被 Supabase 轮询再次打开的问题；同一局结算只展示一次，新局进入 `playing` 后立即关闭旧弹窗。
- 修复下一局沿用上一局 `bet`、`folded` 和旧玩家列表，可能导致刚发牌就错误结算的问题。
- 修复房主破产并转移房主后，新房主客户端身份没有同步的问题。
- 修复中途加入者被误计入当前局存活人数、跟注完成条件、比牌名单和结算名单的问题。
- 修复普通跟注被误判成加注、行动计数反复清零以及“过牌”没有数据变化而无法推进的问题。
- 修复全押筹码不足时被自动弃牌的问题；全押玩家保留获胜资格，结算按累计投入拆分主池和边池。
- 修复加入通知因并发刷新重复出现；结算弹窗每局只展示一次并可手动关闭。
- 修复浏览器刷新后可能生成新匿名身份、原座位变成“假人”的问题；现在会复用 Supabase 登录会话并自动恢复原房间和座位。
- 修复房主操作完成后已切换到下一位玩家，但客户端动作锁没有按轮次变化释放、按钮仍保持禁用的问题；同时阻止并发刷新用旧房间状态覆盖新状态。

## 已增加功能

- 大厅增加“公开房间”，打开时加载一次，并提供手动刷新按钮，不再持续轮询。
- 公开列表显示房号、房间状态和当前人数，可直接加入，不再必须发送邀请码。
- 允许加入已经开始的房间；中途加入者显示“等待下一局”，不参与当前正在进行的牌局。
- 下一局发牌时，所有有筹码的等待玩家会自动转为本局参与者。
- 每位玩家加入时，房间内客户端显示“某某加入了房间”通知；多人连续加入时通知按队列逐条显示。
- 一局结算后展示约 3 秒结果，由当前房主自动开始下一局，无需点击按钮。
- 每个房间最多 8 人，前端加入检查和数据库事务触发器同时限制，避免并发加入突破上限。
- 增加“退出房间”：等待下一局的玩家也可立即退出；普通玩家进行中退出按弃牌处理并在结算后清理；房主进行中退出会退还本局投入、转移房主并回到等待状态。
- 浏览器刷新会自动回到原房间；只有明确点击“退出房间”才清除本机的房间恢复记录。
- 牌桌当前注额旁显示“轮到你”或当前行动玩家姓名，便于确认操作顺序。
- Supabase 每 15 分钟清理一次连续 2 小时无任何玩家、聊天或游戏状态变化的房间。

## Supabase SQL 升级

`zhajinhua_supabase.sql` 已补充并可重复执行：

- `rooms.is_public`：控制公开房间搜索。
- `players.in_round`：区分当前局参与者与等待下一局的玩家。
- `players.compare_with`：补齐比牌目标字段。
- `players.action_seq` / `players.last_action`：记录明确动作，使过牌也能被房主检测。
- `players.all_in`：记录全押状态。
- `players.left_room`：保留中途退出者的本局投入并在结算后清理。
- `enforce_zhajinhua_room_limit()`：使用事务锁将每桌人数硬限制为 8 人。
- `rooms.updated_at` 与活动触发器：记录房间最后操作时间。
- `cleanup_stale_zhajinhua_rooms()` 与 `pg_cron`：自动删除超过 2 小时无活动的房间。
- `messages`：补齐聊天消息表、索引和 RLS 策略。
- RLS 策略和 Realtime publication 改为兼容重复执行。

## 部署顺序

1. 在 Supabase SQL Editor 中执行最新版 `zhajinhua_supabase.sql` 全部内容。旧数据库可直接执行，新增字段使用 `if not exists` 升级。
2. 部署最新版 `zhajinhua.html`。
3. 使用至少两个浏览器会话验证创建、公开搜索、游戏中加入、下一局自动参与和自动开局。

## 本地校验

- JavaScript 已通过 `node --check` 语法检查。
- HTML/SQL 已通过差异空白检查和关键字段引用检查。
- 当前环境未连接并写入线上 Supabase，因此多人实时流程需要在执行 SQL 后进行线上联调。
